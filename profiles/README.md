# Importable uTLS profiles

See root [README.md](../README.md) and [docs/IMPORT.md](../docs/IMPORT.md).

Format: `utls-raw-clienthello-v1` — `clienthello.bin` is the source of truth.

```powershell
../lab.ps1 verify -Id chromium-stable
```

Catalog index: [`../catalog/lab-profiles.json`](../catalog/lab-profiles.json).
